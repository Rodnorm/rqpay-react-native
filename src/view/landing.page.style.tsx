import { StyleSheet, Text, TextStyle, View, ViewStyle } from "react-native";

interface Styles {
  wrapper: ViewStyle;
  // textContent: TextStyle
}

export const LandingPageStyle: Styles = {
  wrapper: {
    marginTop: 2,
  },
};
